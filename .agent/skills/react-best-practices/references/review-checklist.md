# Review Checklist

- check for waterfalls before micro-optimizations
- verify data-fetching boundaries are intentional
- prefer smaller, clearer components over memo soup
- validate that bundle fixes actually reduce shipped code
